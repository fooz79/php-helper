# 版本更新记录

- v0.1.5

trait Singleton 可在抽象类中 use 后在不同的实体类中使用 getSingleton() 时获取各自的单例

- v0.1.6

增加 get_caller_class()

- v0.1.7

更新 Env 对象，支持 loading 时加入二级配置来覆盖主配置 .env.{name}

- v0.1.8

增加 Config，读取 /config 目录下的 php 配置文件
